import 'package:flutter/material.dart';

class VaultSystemScreen extends StatelessWidget {
  const VaultSystemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
